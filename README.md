# 🤖 Telegram Бот для Общения с Клиентами

Современный и удобный Telegram-бот для коммуникации с клиентами через одного администратора.

## ✨ Основные Возможности

### 1. 💬 Ответы на сообщения клиентов

**Простой и понятный процесс:**

1. ✅ Клиент пишет сообщение
2. ✅ Вы получаете уведомление с кнопками "✉️ Ответить" и "📜 История"
3. ✅ Нажимаете кнопку "✉️ Ответить"
4. ✅ Появляется приглашающее сообщение: *"✍️ Ответ для клиента... Напишите следующее текстовое сообщение..."*
5. ✅ Пишете текст ответа и отправляете
6. ✅ Сообщение автоматически доставляется клиенту
7. ✅ Приглашающее сообщение автоматически удаляется
8. ✅ Появляется подтверждение: *"✅ Сообщение отправлено клиенту"*

### 2. 📜 История переписки

- Просмотр полной истории переписки с любым клиентом
- Кнопка быстрого доступа к истории прямо в уведомлении
- Команда `/history <user_id>` для детального просмотра
- Команда `/clients` для списка всех клиентов

## 🚀 Установка

### Требования

- Python 3.10 или выше
- VPS или сервер (для круглосуточной работы)

### Шаг 1: Клонирование проекта

```bash
git clone <ваш-репозиторий>
cd bot_tg
```

### Шаг 2: Создание виртуального окружения

```bash
python3 -m venv .venv
source .venv/bin/activate
```

### Шаг 3: Установка зависимостей

```bash
pip install -r requirements.txt
```

### Шаг 4: Настройка бота

1. Создайте бота через [@BotFather](https://t.me/BotFather)
2. Получите токен бота
3. Узнайте свой Telegram ID (можно через [@userinfobot](https://t.me/userinfobot))

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=ваш_токен_от_BotFather
ADMIN_CHAT_ID=ваш_telegram_id
```

## 🎯 Запуск

### Простой запуск

```bash
python bot.py
```

### Запуск с логированием в файл

```bash
LOG_FILE=/var/log/telegram-bot.log python bot.py
```

### Автозапуск через systemd (рекомендуется)

Создайте файл `/etc/systemd/system/telegram-bot.service`:

```ini
[Unit]
Description=Telegram Client Support Bot
After=network.target

[Service]
Type=simple
User=ваш_пользователь
WorkingDirectory=/путь/к/bot_tg
Environment="BOT_TOKEN=ваш_токен"
Environment="ADMIN_CHAT_ID=ваш_id"
ExecStart=/путь/к/bot_tg/.venv/bin/python /путь/к/bot_tg/bot.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Активируйте сервис:

```bash
sudo systemctl daemon-reload
sudo systemctl enable telegram-bot.service
sudo systemctl start telegram-bot.service
sudo systemctl status telegram-bot.service
```

## 📖 Использование

### Для клиентов

Клиенты просто пишут боту сообщения:
- Текст
- Фото с подписью
- Документы
- Голосовые сообщения
- Видео

### Для администратора

#### Команды:

- `/start` - Приветствие и список доступных команд
- `/clients` - Показать список всех клиентов с кнопками быстрого доступа
- `/history <user_id>` - Показать историю переписки с клиентом
- `/history <user_id> <лимит>` - Показать последние N сообщений (макс. 100)

#### Кнопки в уведомлениях:

- **✉️ Ответить** - Начать ответ клиенту (появится приглашение, следующее сообщение отправится клиенту)
- **📜 История** - Показать последние 20 сообщений с клиентом

## 🗄️ База данных

Все сообщения сохраняются в SQLite базу данных `data/bot.db`:

- История всех сообщений
- Информация о клиентах (ID, username, полное имя)
- Типы сообщений (text, photo, document, voice, video)
- Временные метки

## 🔧 Техническая информация

### Структура проекта

```
bot_tg/
├── bot.py              # Главный файл бота (полностью переписан)
├── database.py         # Работа с SQLite базой данных
├── requirements.txt    # Python зависимости
├── .env               # Конфигурация (создать вручную)
└── data/
    └── bot.db         # База данных (создается автоматически)
```

### Используемые библиотеки

- `python-telegram-bot==20.7` - Библиотека для работы с Telegram Bot API
- `python-dotenv==1.0.1` - Загрузка переменных окружения из .env

## 🆕 Что нового в этой версии?

**Полностью переписанный код:**

✅ Чистая и понятная архитектура
✅ Улучшенная логика ответов на сообщения
✅ Автоматическое удаление приглашающих сообщений
✅ Красивые эмодзи в интерфейсе
✅ Две кнопки под каждым сообщением (Ответить + История)
✅ Подробные логи для отладки
✅ Поддержка различных типов медиа
✅ Улучшенная обработка ошибок

## 📝 Логирование

Бот логирует все важные события:
- Получение сообщений от клиентов
- Отправка ответов
- Просмотр истории
- Ошибки при отправке

По умолчанию логи выводятся в консоль. Для сохранения в файл используйте переменную окружения `LOG_FILE`.

## 🔒 Безопасность

- Только указанный администратор может управлять ботом
- Все данные хранятся локально в SQLite
- Токен бота хранится в `.env` файле (не коммитится в Git)

## 💡 Советы

1. **Резервное копирование**: Регулярно создавайте копии файла `data/bot.db`
2. **Мониторинг**: Используйте `journalctl -u telegram-bot.service -f` для просмотра логов
3. **Обновление**: При обновлении кода перезапустите сервис: `sudo systemctl restart telegram-bot.service`

## 🐛 Решение проблем

**Бот не отвечает:**
- Проверьте, что бот запущен: `sudo systemctl status telegram-bot.service`
- Проверьте логи: `journalctl -u telegram-bot.service -n 50`

**Ошибка "BOT_TOKEN не установлен":**
- Проверьте файл `.env`
- Убедитесь, что токен корректный

**Не приходят уведомления:**
- Проверьте, что ADMIN_CHAT_ID в `.env` - это ваш корректный Telegram ID

## 📞 Поддержка

При возникновении вопросов проверьте логи бота. Большинство проблем описаны в логах с подробностями.

---

**Версия:** 2.0 (Полностью переписанная)
**Дата обновления:** 2025-11-09
